export type ServiceItem = {
  emoji: string
  title: string
  description: string
}

export type FeatureItem = {
  emoji: string
  title: string
  description: string
}

export type Testimonial = {
  quote: string
  author: string
  role: string
}

export type Project = {
  title: string
  description: string
  tags: string[]
}